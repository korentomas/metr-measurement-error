# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Fitted posteriors are too large for git (~450-500MB each; GitHub blocks
# files >100MB). They're attached as GitHub Release assets instead -- see
# "Fitted posteriors" in the README.
outputs/fit_*.nc
